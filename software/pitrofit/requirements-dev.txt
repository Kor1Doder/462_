# Development / CI tooling. Not needed on the deployed Raspberry Pi appliance.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Version floors mirror the [dependency-groups] dev table in pyproject.toml.

-r requirements.txt

ruff>=0.7.4
mypy>=1.13
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=5.0
hypothesis>=6.115
pre-commit>=4.0
